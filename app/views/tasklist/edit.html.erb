<h1>id: <%= @message.id %> のタスク管理ページ</h1>

<%= form_for(@message) do |f| %>
  <%= f.label :content, 'タスク' %>
  <%= f.text_field :content %>

  <%= f.submit '登録' %>
<% end %>

<%= link_to '一覧に戻る', messages_path %>